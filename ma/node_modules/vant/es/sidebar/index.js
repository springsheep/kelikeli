import { use } from '../utils';
import { ParentMixin } from '../mixins/relation';

var _use = use('sidebar'),
    sfc = _use[0],
    bem = _use[1];

export default sfc({
  mixins: [ParentMixin('vanSidebar')],
  props: {
    activeKey: {
      type: [Number, String],
      default: 0
    }
  },
  render: function render(h) {
    return h("div", {
      "class": [bem(), 'van-hairline--top-bottom']
    }, [this.slots()]);
  }
});